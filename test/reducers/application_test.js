
/*

var assert = require('assert');

describe('application reducer', () => {
    describe('application', () => {
        it('should return a new arr ', () => {
            const state = {
                id: action.id,
                title: action.text,
                completed: false

            };

            const action = {

            };

            const expected = {

            };

            assert.deepEqual(application(state, action), expected)

        });
    });
});
*/
